# Usage

```ts
import type { Answer } from "./types";
import { answer } from "./index";
console.log(answer);
```

The same name twice, in two examples:

```javascript
const lib = require("./index");
```

~~~jsx
const lib = require("./missing");
~~~

```python
import os
```

```
import "./untagged";
```

<!-- TUnit: [Test, RulebearingDataSource("rulebearing.yaml")] yields one test per rule.
     Plan: docs/plans/pending/0002-wave-2-dotnet-python-element-rules.md, Step 14 (2H). -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <PackageId>Rulebearing.TestAdapter.TUnit</PackageId>
    <Description>TUnit data source for Rulebearing: [Test, RulebearingDataSource("rulebearing.yaml")] runs one test per architecture rule, failing with the rule's fix text and first violations.</Description>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="../Rulebearing.TestAdapter/Rulebearing.TestAdapter.csproj" />
    <PackageReference Include="TUnit.Core" Version="$(TUnitVersion)" />
    <PackageReference Include="TUnit.Assertions" Version="$(TUnitVersion)" />
  </ItemGroup>
</Project>

<!-- xUnit v2: [Theory, RulebearingRules("rulebearing.yaml")] yields one test per rule.
     Plan: docs/plans/pending/0002-wave-2-dotnet-python-element-rules.md, Step 14 (2H). -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <PackageId>Rulebearing.TestAdapter.xUnit</PackageId>
    <Description>xUnit v2 data source for Rulebearing: [Theory, RulebearingRules("rulebearing.yaml")] runs one test per architecture rule, failing with the rule's fix text and first violations.</Description>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="../Rulebearing.TestAdapter/Rulebearing.TestAdapter.csproj" />
    <PackageReference Include="xunit.extensibility.core" Version="$(XunitV2Version)" />
    <PackageReference Include="xunit.assert" Version="$(XunitV2Version)" />
  </ItemGroup>
</Project>

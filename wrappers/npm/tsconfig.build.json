{
  "//": "Compiles src/ to dist/ for the published package (npm run build). Linting uses the root tsconfig.json; this file is not named tsconfig.json so the eslint project service keeps using the root one. Declarations are emitted because the package exports the launcher to eslint-plugin-rulebearing, which finds the binary through resolveBinary.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "declaration": true,
    "sourceMap": false,
    "types": ["node"]
  },
  "include": ["src/**/*.ts"]
}

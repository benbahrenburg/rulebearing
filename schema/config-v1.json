{
  "$defs": {
    "BuiltInModules": {
      "additionalProperties": false,
      "description": "`builtInModules`.",
      "properties": {
        "add": {
          "description": "Adds to the runtime's list.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "override": {
          "description": "Replaces the runtime's list.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "CachedInputFileSystem": {
      "additionalProperties": false,
      "description": "`enhancedResolveOptions.cachedInputFileSystem`.",
      "properties": {
        "cacheDuration": {
          "description": "Milliseconds a cached entry stays valid.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "CompoundFilter": {
      "additionalProperties": false,
      "description": "The compound form of a path filter.",
      "properties": {
        "dependencyTypes": {
          "description": "Dependency types to match (`doNotFollow`).",
          "items": {
            "$ref": "#/$defs/DependencyType"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "dynamic": {
          "description": "Whether to match dynamic imports (`exclude`).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "path": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Paths to match."
        }
      },
      "type": "object"
    },
    "Define": {
      "additionalProperties": false,
      "description": "`defines.<name>`: a value read from a JSON file\n([FR-CFG-04](../../../docs/prd.md#fr-cfg-04)).",
      "properties": {
        "fromJson": {
          "description": "The JSON file, relative to the configuration file.",
          "type": "string"
        },
        "joinWith": {
          "description": "Joins an array of strings. Default `|`.",
          "type": [
            "string",
            "null"
          ]
        },
        "select": {
          "description": "A path expression: keys, `[*]`, `[n]`, separated by dots.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "fromJson"
      ],
      "type": "object"
    },
    "DependencyRulesSchema": {
      "additionalProperties": false,
      "description": "`rules.dependencies`.",
      "properties": {
        "allowed": {
          "default": null,
          "description": "The only edges that may exist.",
          "items": {
            "$ref": "#/$defs/Rule"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "allowedSeverity": {
          "anyOf": [
            {
              "$ref": "#/$defs/Severity"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The severity of an edge outside `allowed`. Default `warn`."
        },
        "forbidden": {
          "default": null,
          "description": "Edges that must not exist.",
          "items": {
            "$ref": "#/$defs/Rule"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "required": {
          "default": null,
          "description": "Edges that must exist.",
          "items": {
            "$ref": "#/$defs/Rule"
          },
          "type": [
            "array",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "DependencyType": {
      "enum": [
        "aliased-subpath-import",
        "aliased-tsconfig-base-url",
        "aliased-tsconfig-paths",
        "aliased-tsconfig",
        "aliased-webpack",
        "aliased-workspace",
        "aliased",
        "amd-define",
        "amd-require",
        "amd-exotic-require",
        "core",
        "deprecated",
        "dynamic-import",
        "exotic-require",
        "export",
        "import-equals",
        "import",
        "jsdoc",
        "jsdoc-bracket-import",
        "jsdoc-import-tag",
        "local",
        "localmodule",
        "npm-bundled",
        "npm-dev",
        "npm-no-pkg",
        "npm-optional",
        "npm-peer",
        "npm-unknown",
        "npm",
        "pre-compilation-only",
        "process-get-builtin-module",
        "require",
        "triple-slash-amd-dependency",
        "triple-slash-directive",
        "triple-slash-file-reference",
        "triple-slash-type-reference",
        "type-import",
        "type-only",
        "undetermined",
        "unknown",
        "project",
        "package",
        "framework",
        "test-only",
        "signature-only",
        "unresolved",
        "stdlib",
        "site",
        "dynamic"
      ],
      "type": "string"
    },
    "DirectoryFilter": {
      "additionalProperties": false,
      "description": "One `languages.dotnet.directories` entry.",
      "properties": {
        "dir": {
          "description": "The folder, relative to the working directory.",
          "type": "string"
        },
        "filter": {
          "description": "A file-name glob such as `MyApp.*.dll`. Default: `*.dll`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "dir"
      ],
      "type": "object"
    },
    "DotnetOptions": {
      "additionalProperties": false,
      "description": "`languages.dotnet`.",
      "properties": {
        "assemblies": {
          "description": "Assemblies to read instead of a solution's projects, as globs relative to the working\ndirectory (`ArchLoader.LoadAssembly`, `LoadAssemblies`).",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "configuration": {
          "description": "The build configuration whose output is read. Default: `Debug`.",
          "type": [
            "string",
            "null"
          ]
        },
        "directories": {
          "description": "Folders whose assemblies are read, each with an optional file-name filter\n(`LoadFilteredDirectory`).",
          "items": {
            "$ref": "#/$defs/DirectoryFilter"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "excludeProjects": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Project paths to leave out, as patterns."
        },
        "includeDependencies": {
          "description": "Also read the assemblies the loaded ones reference, when they sit beside them\n(`LoadAssembliesIncludingDependencies`). Default: `false`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "namespaces": {
          "description": "Keep only types in these namespaces or below (`LoadNamespacesWithinAssembly`).",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "solution": {
          "description": "The `.sln` or `.slnx` to read. Default: the only one in the working directory.",
          "type": [
            "string",
            "null"
          ]
        },
        "targetFramework": {
          "description": "The target framework to read when a project has several. Default: the first listed.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "EnhancedResolveOptions": {
      "additionalProperties": false,
      "description": "`enhancedResolveOptions`: the resolver settings `oxc_resolver` receives.",
      "properties": {
        "aliasFields": {
          "description": "`package.json` fields holding browser-style alias maps.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "cachedInputFileSystem": {
          "anyOf": [
            {
              "$ref": "#/$defs/CachedInputFileSystem"
            },
            {
              "type": "null"
            }
          ],
          "description": "The file-system cache settings."
        },
        "conditionNames": {
          "description": "Conditions to match in an export map.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "exportsFields": {
          "description": "`package.json` fields holding export maps.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "extensions": {
          "description": "Extensions to try.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "mainFields": {
          "description": "`package.json` fields naming the entry point.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "mainFiles": {
          "description": "File names tried for a directory.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "Examples": {
      "additionalProperties": false,
      "description": "`examples`: `\"from -> to\"` strings.",
      "properties": {
        "allowed": {
          "description": "Edges the rule must allow.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "forbidden": {
          "description": "Edges the rule must flag.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ExtendsValue": {
      "anyOf": [
        {
          "description": "One entry.",
          "type": "string"
        },
        {
          "description": "Several, merged in order.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      ],
      "description": "`extends`: one entry or several."
    },
    "ExternalModuleResolutionStrategy": {
      "enum": [
        "node_modules",
        "yarn-pnp"
      ],
      "type": "string"
    },
    "FileReference": {
      "additionalProperties": false,
      "description": "A configuration file the extractor reads: `tsConfig`, `babelConfig`.",
      "properties": {
        "fileName": {
          "description": "Path, relative to the working directory.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "FilterOption": {
      "description": "`focus`, `reaches`, `highlight`: the pattern form or the compound form.",
      "properties": {
        "depth": {
          "description": "`focus.depth`: how many steps of neighbours to show. Default 1.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "path": {
          "description": "The paths the filter matches, joined.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "FilterValue": {
      "anyOf": [
        {
          "description": "One pattern.",
          "type": "string"
        },
        {
          "description": "Several patterns, joined with `|`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "$ref": "#/$defs/FilterOption",
          "description": "The object form."
        }
      ],
      "description": "A filter in any of its three forms."
    },
    "FromRestriction": {
      "description": "`from`.",
      "properties": {
        "orphan": {
          "description": "Match modules with no edges in or out.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "path": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Paths the source must match."
        },
        "pathNot": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Paths the source must not match."
        }
      },
      "type": "object"
    },
    "IndependenceShorthand": {
      "additionalProperties": false,
      "description": "`rules.independence[]`: one `$1` fence\n([design § Shorthands](../../../docs/artifacts/design.md#shorthands)).",
      "properties": {
        "allowEmpty": {
          "description": "Opt out of liveness.",
          "type": "boolean"
        },
        "comment": {
          "description": "Why the modules are independent.",
          "type": [
            "string",
            "null"
          ]
        },
        "fix": {
          "description": "The fix.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "The rule name.",
          "type": "string"
        },
        "pattern": {
          "description": "A path pattern with exactly one capturing group naming the module, for example\n`^apps/web/src/features/([^/]+)/`.",
          "type": "string"
        },
        "severity": {
          "anyOf": [
            {
              "$ref": "#/$defs/Severity"
            },
            {
              "type": "null"
            }
          ],
          "description": "Severity. Default `error`."
        }
      },
      "required": [
        "name",
        "pattern"
      ],
      "type": "object"
    },
    "KnownRule": {
      "description": "The rule of a `knownViolations` entry.",
      "properties": {
        "name": {
          "description": "The rule name.",
          "type": "string"
        },
        "severity": {
          "anyOf": [
            {
              "$ref": "#/$defs/Severity"
            },
            {
              "type": "null"
            }
          ],
          "description": "The severity it had."
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "KnownViolation": {
      "description": "One `knownViolations` entry: dependency-cruiser's shape, or a Rulebearing id with lifecycle\nfields.",
      "properties": {
        "cycle": {
          "description": "The cycle, for a cycle violation.",
          "items": true,
          "type": [
            "array",
            "null"
          ]
        },
        "expires": {
          "description": "The last day the exception applies.",
          "type": [
            "string",
            "null"
          ]
        },
        "from": {
          "description": "The violating module.",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "The stable id, when Rulebearing wrote the entry.",
          "type": [
            "string",
            "null"
          ]
        },
        "owner": {
          "description": "Who answers for the exception.",
          "type": [
            "string",
            "null"
          ]
        },
        "rule": {
          "anyOf": [
            {
              "$ref": "#/$defs/KnownRule"
            },
            {
              "type": "null"
            }
          ],
          "description": "The rule."
        },
        "to": {
          "description": "The module depended on.",
          "type": [
            "string",
            "null"
          ]
        },
        "via": {
          "description": "The via chain, for a reachability violation.",
          "items": true,
          "type": [
            "array",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "LayersShorthand": {
      "additionalProperties": false,
      "description": "`rules.layers[]`: one `forbidden` rule per lower-to-higher pair\n([design § Shorthands](../../../docs/artifacts/design.md#shorthands)).",
      "properties": {
        "allowEmpty": {
          "description": "Opt out of liveness for every expanded rule.",
          "type": "boolean"
        },
        "comment": {
          "description": "Why the layering exists.",
          "type": [
            "string",
            "null"
          ]
        },
        "fix": {
          "description": "The fix shared by every expanded rule.",
          "type": [
            "string",
            "null"
          ]
        },
        "layers": {
          "description": "Path patterns from the highest layer to the lowest; a lower layer may not depend on a\nhigher one.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "name": {
          "description": "The rule name; each expanded rule is `<name>:<lower>-to-<higher>`.",
          "type": "string"
        },
        "severity": {
          "anyOf": [
            {
              "$ref": "#/$defs/Severity"
            },
            {
              "type": "null"
            }
          ],
          "description": "Severity of every expanded rule. Default `error`."
        }
      },
      "required": [
        "name",
        "layers"
      ],
      "type": "object"
    },
    "ModuleRestriction": {
      "description": "`module`: the restriction of a dependents rule or a required rule.",
      "properties": {
        "numberOfDependentsLessThan": {
          "description": "Match modules with fewer matching dependents than this.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "numberOfDependentsMoreThan": {
          "description": "Match modules with more matching dependents than this.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "path": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Modules to match."
        },
        "pathNot": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Modules not to match."
        }
      },
      "type": "object"
    },
    "ModuleSystem": {
      "enum": [
        "cjs",
        "es6",
        "amd",
        "tsd",
        "clr",
        "py"
      ],
      "type": "string"
    },
    "NativeLanguages": {
      "additionalProperties": false,
      "description": "`languages`.",
      "properties": {
        "dotnet": {
          "anyOf": [
            {
              "$ref": "#/$defs/DotnetOptions"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The .NET extractor's options (wave 2)."
        },
        "python": {
          "anyOf": [
            {
              "$ref": "#/$defs/PythonOptions"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The Python extractor's options (wave 2)."
        },
        "typescript": {
          "anyOf": [
            {
              "$ref": "#/$defs/TypeScriptOptions"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The TypeScript and JavaScript extractor's options (dependency-cruiser's flat option names)."
        }
      },
      "type": "object"
    },
    "NativeRules": {
      "additionalProperties": false,
      "description": "`rules`.",
      "properties": {
        "dependencies": {
          "anyOf": [
            {
              "$ref": "#/$defs/DependencyRulesSchema"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "dependency-cruiser's rule set."
        },
        "independence": {
          "default": null,
          "description": "Independence contracts, expanded to one `$1` fence each.",
          "items": {
            "$ref": "#/$defs/IndependenceShorthand"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "layers": {
          "default": null,
          "description": "Layer models, expanded to one `forbidden` rule per lower-to-higher pair.",
          "items": {
            "$ref": "#/$defs/LayersShorthand"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "ratchets": {
          "default": null,
          "description": "Counts of direct edges that may only fall.",
          "items": {
            "$ref": "#/$defs/Ratchet"
          },
          "type": [
            "array",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "OptionsSchema": {
      "description": "`options`: the TypeScript block's keys and every other dependency-cruiser option.",
      "properties": {
        "babelConfig": {
          "anyOf": [
            {
              "$ref": "#/$defs/FileReference"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Babel config whose aliases apply."
        },
        "baseDir": {
          "description": "Paths in the output are relative to this directory. Default: the working directory.",
          "type": [
            "string",
            "null"
          ]
        },
        "builtInModules": {
          "anyOf": [
            {
              "$ref": "#/$defs/BuiltInModules"
            },
            {
              "type": "null"
            }
          ],
          "description": "Changes to the runtime's built-in module list."
        },
        "cache": {
          "default": null,
          "description": "Accepted and recorded; the content-addressed cache is wave 3."
        },
        "collapse": {
          "default": null,
          "description": "`collapse`: a pattern or a folder depth."
        },
        "combinedDependencies": {
          "description": "Whether every `package.json` up the tree counts for npm classification. Default: `false`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "detectJSDocImports": {
          "description": "Whether JSDoc imports are dependencies. Default: `false`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "detectProcessBuiltinModuleCalls": {
          "description": "Whether `process.getBuiltinModule` calls are dependencies. Default: `false`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "doNotFollow": {
          "anyOf": [
            {
              "$ref": "#/$defs/PathFilter"
            },
            {
              "type": "null"
            }
          ],
          "description": "Modules to record but not follow."
        },
        "enhancedResolveOptions": {
          "anyOf": [
            {
              "$ref": "#/$defs/EnhancedResolveOptions"
            },
            {
              "type": "null"
            }
          ],
          "description": "Resolver settings."
        },
        "exclude": {
          "anyOf": [
            {
              "$ref": "#/$defs/PathFilter"
            },
            {
              "type": "null"
            }
          ],
          "description": "Modules to leave out entirely."
        },
        "exoticRequireStrings": {
          "description": "Names other than `require` that load a module.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "experimentalStats": {
          "description": "Whether to record size and statement counts. Default: `false`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "externalModuleResolutionStrategy": {
          "anyOf": [
            {
              "$ref": "#/$defs/ExternalModuleResolutionStrategy"
            },
            {
              "type": "null"
            }
          ],
          "description": "`node_modules` or Yarn Plug'n'Play. Default: `node_modules`."
        },
        "extraExtensionsToScan": {
          "description": "Extensions to scan beyond the parser's own.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "focus": {
          "anyOf": [
            {
              "$ref": "#/$defs/FilterValue"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`focus`: a pattern, a list, or `{ path, depth }`."
        },
        "forceDeriveDependents": {
          "default": null,
          "description": "Always derive `dependents[]`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "highlight": {
          "anyOf": [
            {
              "$ref": "#/$defs/FilterValue"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`highlight`: a pattern, a list, or `{ path }`."
        },
        "includeOnly": {
          "anyOf": [
            {
              "$ref": "#/$defs/PathFilter"
            },
            {
              "type": "null"
            }
          ],
          "description": "The only modules to include."
        },
        "knownViolations": {
          "default": null,
          "description": "Violations to report at a lower severity, keyed by stable id or by rule, from and to.",
          "items": {
            "$ref": "#/$defs/KnownViolation"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "maxDepth": {
          "description": "How deep to follow from the roots; `0` is unlimited. Default: `0`.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "metrics": {
          "default": null,
          "description": "Compute instability metrics (wave 2).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "moduleSystems": {
          "description": "Which dependency forms to extract. Default: `es6`, `cjs`, `tsd`, `amd`.",
          "items": {
            "$ref": "#/$defs/ModuleSystem"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "parser": {
          "anyOf": [
            {
              "$ref": "#/$defs/Parser"
            },
            {
              "type": "null"
            }
          ],
          "description": "Which parser dependency-cruiser would have used. Default: `acorn`, or `tsc` for\nTypeScript sources."
        },
        "prefix": {
          "default": null,
          "description": "The link prefix reporters put before a path.",
          "type": [
            "string",
            "null"
          ]
        },
        "preserveSymlinks": {
          "description": "Whether symlinks keep their own path. Default: `false`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "progress": {
          "default": null,
          "description": "`{ type: none | cli-feedback | performance-log | ndjson }`."
        },
        "reaches": {
          "anyOf": [
            {
              "$ref": "#/$defs/FilterValue"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`reaches`: a pattern, a list, or `{ path }`."
        },
        "reporterOptions": {
          "default": null,
          "description": "Per-reporter settings."
        },
        "skipAnalysisNotInRules": {
          "default": null,
          "description": "Skip derivations no rule reads.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "suffix": {
          "default": null,
          "description": "The link suffix reporters put after a path.",
          "type": [
            "string",
            "null"
          ]
        },
        "tsConfig": {
          "anyOf": [
            {
              "$ref": "#/$defs/FileReference"
            },
            {
              "type": "null"
            }
          ],
          "description": "The tsconfig to resolve `paths` and `baseUrl` from."
        },
        "tsPreCompilationDeps": {
          "anyOf": [
            {
              "$ref": "#/$defs/TsPreCompilationDeps"
            },
            {
              "type": "null"
            }
          ],
          "description": "Whether edges that disappear after compilation are kept. Default: `false`."
        },
        "webpackConfig": {
          "anyOf": [
            {
              "$ref": "#/$defs/WebpackConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "The webpack config whose `resolve` applies."
        }
      },
      "type": "object"
    },
    "Parser": {
      "enum": [
        "acorn",
        "tsc",
        "swc"
      ],
      "type": "string"
    },
    "PathFilter": {
      "anyOf": [
        {
          "$ref": "#/$defs/Patterns",
          "description": "The short form, patterns only."
        },
        {
          "$ref": "#/$defs/CompoundFilter",
          "description": "The compound form."
        }
      ],
      "description": "`doNotFollow`, `exclude` and `includeOnly`: either patterns or the compound form."
    },
    "Patterns": {
      "anyOf": [
        {
          "description": "A single expression.",
          "type": "string"
        },
        {
          "description": "Several expressions, matched as an alternation.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      ],
      "description": "One regular expression or several, dependency-cruiser's `REAsStringsType`."
    },
    "PythonOptions": {
      "additionalProperties": false,
      "description": "`languages.python`.",
      "properties": {
        "roots": {
          "description": "Import roots. Default: `src` when it exists, else the working directory.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "stubs": {
          "description": "Whether `.pyi` stubs are modules. Default: `false`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "version": {
          "description": "The Python version whose standard library is `stdlib`. Default: `3.13`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "Ratchet": {
      "additionalProperties": false,
      "description": "`rules.ratchets[]`: a count of direct edges that may only fall\n([FR-RULE-06](../../../docs/prd.md#fr-rule-06)).",
      "properties": {
        "budget": {
          "description": "The budget file, `{ \"ceiling\": n }`.",
          "type": "string"
        },
        "comment": {
          "description": "Why it exists.",
          "type": [
            "string",
            "null"
          ]
        },
        "fix": {
          "description": "The imperative to follow when the count rises.",
          "type": [
            "string",
            "null"
          ]
        },
        "from": {
          "$ref": "#/$defs/FromRestriction",
          "description": "Sources to count from."
        },
        "name": {
          "description": "The ratchet name.",
          "type": "string"
        },
        "owner": {
          "description": "Who answers for it.",
          "type": [
            "string",
            "null"
          ]
        },
        "to": {
          "$ref": "#/$defs/ToRestriction",
          "description": "Targets to count to; `$1` substitutes from `from.path`."
        }
      },
      "required": [
        "name",
        "from",
        "to",
        "budget"
      ],
      "type": "object"
    },
    "Rule": {
      "description": "One dependency rule, in dependency-cruiser's shape plus the native metadata.",
      "properties": {
        "allowEmpty": {
          "description": "Opt out of liveness ([ADR-0007](../../../docs/adr/0007-vacuous-rules-fail-by-default.md)).",
          "type": "boolean"
        },
        "comment": {
          "description": "Why the rule exists; carries the decision token.",
          "type": [
            "string",
            "null"
          ]
        },
        "examples": {
          "anyOf": [
            {
              "$ref": "#/$defs/Examples"
            },
            {
              "type": "null"
            }
          ],
          "description": "Edges that must pass and edges that must fail; `rulebearing test` runs them."
        },
        "expires": {
          "description": "The last day the rule applies; the run fails the day after.",
          "type": [
            "string",
            "null"
          ]
        },
        "fix": {
          "description": "The imperative to follow when the rule fires.",
          "type": [
            "string",
            "null"
          ]
        },
        "from": {
          "$ref": "#/$defs/FromRestriction",
          "default": {},
          "description": "The selecting side of a dependency rule."
        },
        "module": {
          "anyOf": [
            {
              "$ref": "#/$defs/ModuleRestriction"
            },
            {
              "type": "null"
            }
          ],
          "description": "The module restriction of a dependents or required rule."
        },
        "name": {
          "description": "The rule name. Default `unnamed`.",
          "type": [
            "string",
            "null"
          ]
        },
        "owner": {
          "description": "Who answers for the rule.",
          "type": [
            "string",
            "null"
          ]
        },
        "scope": {
          "anyOf": [
            {
              "$ref": "#/$defs/Scope"
            },
            {
              "type": "null"
            }
          ],
          "description": "`module` or `folder`. Default `module`."
        },
        "severity": {
          "anyOf": [
            {
              "$ref": "#/$defs/Severity"
            },
            {
              "type": "null"
            }
          ],
          "description": "`error`, `warn`, `info` or `ignore`. Default `warn`."
        },
        "to": {
          "$ref": "#/$defs/ToRestriction",
          "default": {},
          "description": "The target side."
        }
      },
      "type": "object"
    },
    "Scope": {
      "description": "`scope`: module or folder.",
      "oneOf": [
        {
          "const": "module",
          "description": "The default: the rule compares modules.",
          "type": "string"
        },
        {
          "const": "folder",
          "description": "The rule compares folders.",
          "type": "string"
        }
      ]
    },
    "Severity": {
      "enum": [
        "ignore",
        "info",
        "warn",
        "error"
      ],
      "type": "string"
    },
    "SpecifyLiteral": {
      "description": "The literal string `\"specify\"`.",
      "oneOf": [
        {
          "const": "specify",
          "description": "`\"specify\"`.",
          "type": "string"
        }
      ]
    },
    "ToRestriction": {
      "description": "`to`: every restriction dependency-cruiser 18.2.0 defines.",
      "properties": {
        "ancestor": {
          "description": "Match targets in a folder above the source.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "circular": {
          "description": "Match edges on a cycle.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "couldNotResolve": {
          "description": "Match unresolvable targets.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "dependencyTypes": {
          "description": "Dependency types, any of which matches.",
          "items": {
            "$ref": "#/$defs/DependencyType"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "dependencyTypesNot": {
          "description": "Dependency types, none of which may be present.",
          "items": {
            "$ref": "#/$defs/DependencyType"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "dynamic": {
          "description": "Match `import()` edges.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "exoticRequire": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "The exotic require names to match."
        },
        "exoticRequireNot": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "The exotic require names not to match."
        },
        "exoticallyRequired": {
          "description": "Match edges formed by an exotic require.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "license": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Licences to match (wave 2)."
        },
        "licenseNot": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Licences not to match (wave 2)."
        },
        "moreThanOneDependencyType": {
          "description": "Match edges whose target is depended on in more than one way.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "moreUnstable": {
          "description": "Match targets more unstable than the source (needs `metrics`, wave 2).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "path": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Paths the target must match; `$0` to `$9` substitute captures from `from.path`."
        },
        "pathNot": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Paths the target must not match."
        },
        "preCompilationOnly": {
          "description": "Match edges that disappear after compilation.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "reachable": {
          "description": "Reachability: match modules the `from` modules reach (or do not reach).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "via": {
          "anyOf": [
            {
              "$ref": "#/$defs/ViaRestriction"
            },
            {
              "type": "null"
            }
          ],
          "description": "Cycle members, some of which match."
        },
        "viaNot": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Shorthand for `viaOnly.pathNot`."
        },
        "viaOnly": {
          "anyOf": [
            {
              "$ref": "#/$defs/ViaRestriction"
            },
            {
              "type": "null"
            }
          ],
          "description": "Cycle members, all of which match."
        },
        "viaSomeNot": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Shorthand for `via.pathNot`."
        }
      },
      "type": "object"
    },
    "TsPreCompilationDeps": {
      "anyOf": [
        {
          "description": "Include (`true`) or drop (`false`) edges that vanish after compilation.",
          "type": "boolean"
        },
        {
          "$ref": "#/$defs/SpecifyLiteral",
          "description": "Include them and mark each with `preCompilationOnly`."
        }
      ],
      "description": "`tsPreCompilationDeps`: `true`, `false` or `\"specify\"`."
    },
    "TypeScriptOptions": {
      "additionalProperties": false,
      "description": "`languages.typescript`, and dependency-cruiser's flat options that alias into it.",
      "properties": {
        "babelConfig": {
          "anyOf": [
            {
              "$ref": "#/$defs/FileReference"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Babel config whose aliases apply."
        },
        "baseDir": {
          "description": "Paths in the output are relative to this directory. Default: the working directory.",
          "type": [
            "string",
            "null"
          ]
        },
        "builtInModules": {
          "anyOf": [
            {
              "$ref": "#/$defs/BuiltInModules"
            },
            {
              "type": "null"
            }
          ],
          "description": "Changes to the runtime's built-in module list."
        },
        "combinedDependencies": {
          "description": "Whether every `package.json` up the tree counts for npm classification. Default: `false`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "detectJSDocImports": {
          "description": "Whether JSDoc imports are dependencies. Default: `false`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "detectProcessBuiltinModuleCalls": {
          "description": "Whether `process.getBuiltinModule` calls are dependencies. Default: `false`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "doNotFollow": {
          "anyOf": [
            {
              "$ref": "#/$defs/PathFilter"
            },
            {
              "type": "null"
            }
          ],
          "description": "Modules to record but not follow."
        },
        "enhancedResolveOptions": {
          "anyOf": [
            {
              "$ref": "#/$defs/EnhancedResolveOptions"
            },
            {
              "type": "null"
            }
          ],
          "description": "Resolver settings."
        },
        "exclude": {
          "anyOf": [
            {
              "$ref": "#/$defs/PathFilter"
            },
            {
              "type": "null"
            }
          ],
          "description": "Modules to leave out entirely."
        },
        "exoticRequireStrings": {
          "description": "Names other than `require` that load a module.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "experimentalStats": {
          "description": "Whether to record size and statement counts. Default: `false`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "externalModuleResolutionStrategy": {
          "anyOf": [
            {
              "$ref": "#/$defs/ExternalModuleResolutionStrategy"
            },
            {
              "type": "null"
            }
          ],
          "description": "`node_modules` or Yarn Plug'n'Play. Default: `node_modules`."
        },
        "extraExtensionsToScan": {
          "description": "Extensions to scan beyond the parser's own.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "includeOnly": {
          "anyOf": [
            {
              "$ref": "#/$defs/PathFilter"
            },
            {
              "type": "null"
            }
          ],
          "description": "The only modules to include."
        },
        "maxDepth": {
          "description": "How deep to follow from the roots; `0` is unlimited. Default: `0`.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "moduleSystems": {
          "description": "Which dependency forms to extract. Default: `es6`, `cjs`, `tsd`, `amd`.",
          "items": {
            "$ref": "#/$defs/ModuleSystem"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "parser": {
          "anyOf": [
            {
              "$ref": "#/$defs/Parser"
            },
            {
              "type": "null"
            }
          ],
          "description": "Which parser dependency-cruiser would have used. Default: `acorn`, or `tsc` for\nTypeScript sources."
        },
        "preserveSymlinks": {
          "description": "Whether symlinks keep their own path. Default: `false`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "tsConfig": {
          "anyOf": [
            {
              "$ref": "#/$defs/FileReference"
            },
            {
              "type": "null"
            }
          ],
          "description": "The tsconfig to resolve `paths` and `baseUrl` from."
        },
        "tsPreCompilationDeps": {
          "anyOf": [
            {
              "$ref": "#/$defs/TsPreCompilationDeps"
            },
            {
              "type": "null"
            }
          ],
          "description": "Whether edges that disappear after compilation are kept. Default: `false`."
        },
        "webpackConfig": {
          "anyOf": [
            {
              "$ref": "#/$defs/WebpackConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "The webpack config whose `resolve` applies."
        }
      },
      "type": "object"
    },
    "ViaObject": {
      "description": "The object form of `via`.",
      "properties": {
        "dependencyTypes": {
          "description": "Dependency types a step must have.",
          "items": {
            "$ref": "#/$defs/DependencyType"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "dependencyTypesNot": {
          "description": "Dependency types a step must not have.",
          "items": {
            "$ref": "#/$defs/DependencyType"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "path": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Cycle members to match."
        },
        "pathNot": {
          "anyOf": [
            {
              "$ref": "#/$defs/Patterns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Cycle members not to match."
        }
      },
      "type": "object"
    },
    "ViaRestriction": {
      "anyOf": [
        {
          "$ref": "#/$defs/Patterns",
          "description": "`via: \"pattern\"` or `via: [\"a\", \"b\"]`."
        },
        {
          "$ref": "#/$defs/ViaObject",
          "description": "`via: { path, pathNot, dependencyTypes, dependencyTypesNot }`."
        }
      ],
      "description": "`via` and `viaOnly`: the pattern form or the object form."
    },
    "WebpackConfig": {
      "additionalProperties": false,
      "description": "`webpackConfig`.",
      "properties": {
        "arguments": {
          "description": "The `arguments` passed to a function-shaped config."
        },
        "env": {
          "description": "The `env` passed to a function-shaped config."
        },
        "fileName": {
          "description": "Path, relative to the working directory.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    }
  },
  "$id": "https://benbahrenburg.github.io/rulebearing/schema/config-v1.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "A native configuration file: `rulebearing.yaml`, `.json`, `.jsonc` or `.toml`.",
  "properties": {
    "$schema": {
      "default": null,
      "description": "The schema this file is written against.",
      "type": [
        "string",
        "null"
      ]
    },
    "allowed": {
      "default": null,
      "description": "dependency-cruiser's `allowed`, legal at the same place.",
      "items": {
        "$ref": "#/$defs/Rule"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "allowedSeverity": {
      "anyOf": [
        {
          "$ref": "#/$defs/Severity"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "dependency-cruiser's `allowedSeverity`, legal at the same place."
    },
    "defines": {
      "additionalProperties": {
        "$ref": "#/$defs/Define"
      },
      "default": null,
      "description": "Named values read from JSON files, substituted into patterns as `${name}`.",
      "type": [
        "object",
        "null"
      ]
    },
    "extends": {
      "anyOf": [
        {
          "$ref": "#/$defs/ExtendsValue"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Files, npm packages, `dependency-cruiser/configs/<preset>` or `rulebearing:<preset>` to\nextend; later entries are merged under earlier ones and this file wins."
    },
    "forbidden": {
      "default": null,
      "description": "dependency-cruiser's `forbidden`, legal at the same place.",
      "items": {
        "$ref": "#/$defs/Rule"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "languages": {
      "anyOf": [
        {
          "$ref": "#/$defs/NativeLanguages"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Per-language settings."
    },
    "options": {
      "anyOf": [
        {
          "$ref": "#/$defs/OptionsSchema"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Every dependency-cruiser option, unchanged."
    },
    "required": {
      "default": null,
      "description": "dependency-cruiser's `required`, legal at the same place.",
      "items": {
        "$ref": "#/$defs/Rule"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "rules": {
      "anyOf": [
        {
          "$ref": "#/$defs/NativeRules"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The rule families."
    }
  },
  "title": "Rulebearing configuration",
  "type": "object"
}
